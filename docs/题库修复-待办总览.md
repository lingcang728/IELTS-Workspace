# 题库修复 · 待办总览

一页看清还剩什么、发给谁、怎么验收。

日期：2026-08-26 · 题库状态：212 卷 · 健康 **95.7%** · 损坏 240 · 缺口 100 · 门通过

Choose TWO/THREE 听力组已合并为 `in_either_order`（95 组）。v1.3.2 仍被 C13 Test 3 Listening 印刷页 54–55 挡住。

---

## 三批可外包的活，可以同时开跑

三批互不重叠，写入的文件也不冲突（前两批写 `fixtures/overlays/`，答案键那批写 `fixtures/answer-keys/`），可以并行派给三个模型。

| 批次 | 手册 | 工单 | 量 |
|---|---|---|---|
| ~~答案键抄写~~ | `docs/答案键抄写-执行手册.md` | — | **已完成 129/129** |
| **选项抄写** | `docs/选项抄写-执行手册.md` | `data-dev/repair/option-tasks/` | **4 单未提交**（看页后不是选择题，见手册） |
| ~~标题表抄写~~ | `docs/标题表抄写-执行手册.md` | — | **已完成 33/33** |

三批的共同点：**对错完全可机检，不靠抽样。** 每批的校验器都是全单拒收，且都带一道"独立第二证人"的硬闸：

- 答案键 → 必须与文字抽取已确认的答案逐条吻合
- 选项 → 答案字母必须出现在提交的选项里
- 标题表 → 答案用到的每个罗马数字都必须在提交的表里

这三道闸都是先修好答案键才换来的。模型编不出来。

### 给模型的话（三批通用）

> 按手册做，逐单自检通过再提交。**不许猜**——页面上没印的一个字都不许写；看不清就不提交这一单，报出来是哪一单、哪几题、为什么。**不要碰 `fixtures/cambridge/`**，不要跑 `40_apply.py` / `69 --apply` / `verify_cambridge.py --update-baseline`。汇报要逐单列，不要写"全部完成"——每一单都会被逐条机检。

---

## 我这边收货后的动作

```
python scripts/repair/40_apply.py            # overlay → fixtures
python scripts/repair/65_mechanical_repairs.py   # 选项到位后题型自动归位
python scripts/repair/67_fix_declared_structure.py
python scripts/verify_cambridge.py --baseline
```

顺序不能反：选项/标题表落地之前跑题型归类是没用的，反过来也一样。

---

## 还没有解法的部分

| 项 | 量 | 卡在哪 |
|---|---|---|
| C04 答案页 | 8 区块 | 那册在答案区的 OCR 没留下任何痕迹，三级定位（页眉 / 标题 / 答案行密度）全部落空。`73_answer_key_worklist.py` 里留了 `PINNED` 常量，人工确认 PDF 页码后填进去即可 |
| ~~地图/图表题缺图~~ | 0 | **已完成 136/136**：`79_attach_map_images.py` 把 MinerU 切图挂到 group.imageAsset，考场 `QuestionGroupView` 会画出来 |
| C13 T3 Listening | Q1–16 | 原书扫描件缺印刷页 54–55，见 `docs/known-missing-scans.md` |
| 剑21 听力 | 全部 | `听力/` 只到剑20，没有音源 |
| 剑04 听力原文 | 4 套 | 没抽出听力原文，精听已按 `hasTranscript` 过滤掉，练习/模考照常可用 |

---

## 走到这里的账

| 阶段 | 损坏 | 缺口 |
|---|---:|---:|
| 第一轮后 | — | 1638 |
| 第二、三轮组级校对 | 245 | 1005 |
| 结构归位（67） | 263 | 638 |
| 换上印刷答案键 | 476 | 535 |
| 补回 355 份选项 + 题型归位 | 249 | 427 |
| 补齐最后 32 套印刷答案键 | 286 | 395 |
| 补 33 张标题表 + 14 单选项 | 243 | 395 |
| 门停报组级空壳假警 | 243 | 236 |
| 挂上 136 组地图切图 + 2 单选项 | **241** | **100** |

中间那次 263 → 476 是**数据变诚实**，不是变坏：旧键错得恰好能和错题型互相圆场，两边一起藏在门后面。键改对之后 413 道缺选项的题露了出来，补完就掉到 249——比掩盖时期还低。

详细根因写在 `docs/题库结构缺陷-交接说明.md`。
