# 题库结构缺陷 · 执行记录

写给审核的模型。日期：2026-08-25。对应脚本：`scripts/repair/67_fix_declared_structure.py`。

---

## 做了什么

交接要求：把 349 个「A–H 全空」题组归到正确题型和真实选项宽度，不猜页面上没印的字，有文字的选项不许删。

脚本只吃已经在题库里的四类证据：overlay `note`、答案键、题干上已有的选项文字、印刷说明。两个来源打架就停手。

| 结果 | 数量 |
|---:|---|
| A–H 空组（开工时） | 349 |
| 已改结构 | 287 |
| 扫描缺页，记为已知缺失 | 16 题 / overlay 16 组（C13 T3 Listening 印刷页 54–55） |
| 仍是 A–H 空（note 与答案键矛盾） | 61 |
| 带文字选项被删 | 0（18254 → 19335，只增不减） |
| `short_answer` → `completion` | 6 |

改结构的两种合法动作：

1. **幽灵 sharedOptions**：题干上已经有完整选项（标题、词库、段落字母、A–D 正文），组级还挂着导入器盖的空 A–H。把组级改成题干那份，不改任何一个已有 `text`。134 组。
2. **按 note×键裁宽度 / 改题型**：matching / single_choice / labelling / completion / yes_no_ng 等。空槽可以裁或扩，文字槽不动。

没有改 `fixtures/overlays/`。每个被写的 `fixtures/cambridge/*.json` 旁有 `.bak`。

---

## 门

```
python scripts/verify_cambridge.py --baseline --health --skip-audio
→ ok: true, damagedQuestions: 263, contentGaps: 502, healthPct: 95.3
```

交接时：damaged 267、gaps 673、95.2%。检测器没变锋利。基线已锁到 263 / 502，见 `fixtures/cambridge-health-baseline.json`。

音频没跑（`--skip-audio`）。没开 App、没跑 `verify.ps1`。

---

## 没做完的 61 组（请审核时盯这里）

每一组 overlay note 都说「这是判断 / 填空 / 标题匹配」，答案键却是拉丁字母。按交接纪律**不能**把 `F` 当成 FALSE、把 `C` 当成罗马数字 `iii`——抽过官方键，那种映射是错的（列偏移，不是编码）。

继续改题型而不改键，会让 damaged 上升（字母答案 + 非 OPTIONED 题型）。所以留下了。它们不是抄写活，是**键修复**活。

分类：

- 20 组：note 说填空，键是字母
- 17 组：note 说 YNNG，键是字母
- 16 组：note 说 TFNG，键是字母
- 6 组：note 说 List of Headings，键是拉丁字母
- 2 组：note 说选词填空但没有印出的宽度，键是字母

逐条在 `data-dev/repair/structure-fix-report.json` 的 `decisions` 里，`action == "refuse"`。

官方键对照（抽查，不是本轮写入）：C18 T1 R 22–26 应为 fire/nutrients/cavities/hawthorn/rare，题库却是字母；C20 T2 R 27–32 应为 NO/YES/NG/NO/NG/YES，题库却是 D/G/B/C/B/D。note 对，键错。

下一轮建议：用答案页（或已引用的独立答案源）修这 61 个键，再重跑 `67_fix_declared_structure.py`。不要再派便宜模型对着页面抄选项。

---

## 已知缺失（不再计入待办）

`docs/known-missing-scans.md`  
`data-dev/repair/known-missing-scans.json`

C13 Test 3 Listening 印刷页 54–55，Questions 1–16。

---

## 审核时建议对的三件事

1. 抽 5 个 `reconcile_shared`：题干选项文字改前改后一致，只是组级空 A–H 没了。
2. 抽 5 个 `set_options`：宽度覆盖答案键，没有任何 `text` 被换成空。
3. 确认 61 条 refuse 没有被偷偷改题型。
