# 听力资源

此目录用于记录项目所有者在本机合法取得的 IELTS 听力材料，但不分发录音文件。

`manifest.csv` 仅保存相对路径、扩展名、字节数和 SHA-256，用来核对本机音频是否完整、是否误用了其他来源。原始音频仍保存在 `C:\Users\15pro\Desktop\听力`，并通过 `.gitignore` 排除。

公开仓库使用者需要自行准备有权使用的录音，并在本地导入；请勿提交 Cambridge 出版物随附的受版权保护音频。
