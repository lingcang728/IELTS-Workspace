# 听力资源 (Listening Materials)

本目录包含 IELTS 备考听力音频资源及数据校验清单。

## 目录结构

- `剑1` ~ `剑20`：按册数组织，每册包含各个 Test（如 `Test1` ~ `Test4`）的各 Section / Part 音频文件（标准 MP3/M4A 格式）。
- `manifest.csv`：所有音频文件的相对路径、格式、文件大小及 SHA-256 哈希校验清单，用于确保数据完整性与一致性。

## 使用说明

IELTS Workspace 运行时及构建脚本会自动索引和处理本目录下的音频数据，用于生成练习与模考所需的媒体流。
