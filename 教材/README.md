# 教材资源

此目录用于记录项目所有者在本机合法取得的 IELTS 教材，但不分发 PDF 文件。

`manifest.csv` 仅保存相对路径、扩展名、字节数和 SHA-256，用来核对本机教材是否完整。原始 PDF 仍保存在 `C:\Users\15pro\Desktop\教材`，并通过 `.gitignore` 排除。

Cambridge 官方声明其出版物原则上不得在未获书面许可时复制或电子分发，因此这些 PDF 不会进入 Git 或 GitHub。
